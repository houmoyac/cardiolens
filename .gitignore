__pycache__/
*.py[cod]
.venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.egg-info/
dist/
build/

# Real patient data must never be committed
*.dat
*.hea
data/patients/

# Local SQLite DB (real account data, even in dev)
*.db
*.db-journal

# Uploaded workplace logos (doctor's own content, not meant to be shipped
# in the repo — same reasoning as the account DB)
user_logos/
