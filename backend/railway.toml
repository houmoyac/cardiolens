[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run streamlit run src/cardiolens/webapp.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
restartPolicyType = "ON_FAILURE"
